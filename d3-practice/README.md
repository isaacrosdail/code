
Just using this file to note down what I should know / what's yet to learn / etc

// Next tiny builds could be:
// Change to donut chart (innerRadius > 0)
// Add percentage labels
// Add transitions between datasets
// Add legend
// Animate slices exploding on hover
// Interactive click-to-hide slices
// Replace static data w/ live API
// Turn into a donut chart (innerRadius > 0)
// Show only category OR only percent
// Push labels slightly outward to avoid overlap
// Add a legend outside the pie
// Add hover effect (slice pops out)